{
  "name": "dolosblackmagic",
  "version": "0.5.0",
  "private": true,
  "description": "Local-first browser DFIR, log intelligence, detection engineering, incident response and SOC operations workbench.",
  "scripts": {
    "test": "node tests/core.test.mjs && node tests/log-engine.test.mjs && node tests/soc.test.mjs && node tests/ops.test.mjs && node scripts/check-static.mjs",
    "serve": "python3 -m http.server 8080 -d site"
  },
  "engines": {"node": ">=24"}
}
