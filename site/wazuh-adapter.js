(function(global){
'use strict';
const VERSION='1.1.0';
const text=v=>String(v??'');
const clean=v=>{const s=text(v).trim();return s==='-'||s==='N/A'||s==='null'?'':s};
function get(obj,path){let value=obj;for(const key of path.split('.')){if(value==null)return undefined;value=value[key]}return value}
function first(obj,paths){for(const path of paths){const value=get(obj,path);if(value!==undefined&&value!==null&&value!=='')return value}return''}
function unwrapHit(value){if(value&&typeof value==='object'&&value._source&&typeof value._source==='object')return{...value._source,__wazuhIndex:value._index||'',__wazuhDocumentId:value._id||''};return value}
function extractRecords(value){if(Array.isArray(value))return value.map(unwrapHit);if(!value||typeof value!=='object')return[value];if(Array.isArray(value?.hits?.hits))return value.hits.hits.map(unwrapHit);if(Array.isArray(value?.data?.affected_items))return value.data.affected_items.map(unwrapHit);if(Array.isArray(value?.data?.items))return value.data.items.map(unwrapHit);if(Array.isArray(value?.affected_items))return value.affected_items.map(unwrapHit);if(Array.isArray(value?.alerts))return value.alerts.map(unwrapHit);if(value._source&&typeof value._source==='object')return[unwrapHit(value)];return[value]}
function looksWazuh(record){record=unwrapHit(record);if(!record||typeof record!=='object')return false;return Boolean(record?.data?.win||(record.rule&&record.agent)||(record.rule&&(record.decoder||record.location||record.manager))||(record.agent&&record.decoder&&record.timestamp))}
function isoMs(v){const n=Date.parse(v||'');return Number.isFinite(n)?n:null}
function canonicalize(input){
 const record=unwrapHit(input)||{};if(!looksWazuh(record))return record;
 const rule=record.rule&&typeof record.rule==='object'?record.rule:{},agent=record.agent&&typeof record.agent==='object'?record.agent:{},decoder=record.decoder&&typeof record.decoder==='object'?record.decoder:{},manager=record.manager&&typeof record.manager==='object'?record.manager:{},ws=record?.data?.win?.system||{},we=record?.data?.win?.eventdata||{};
 const eventTimestamp=first(record,['data.win.system.systemTime','@timestamp','timestamp','data.timestamp']);
 const alertTimestamp=first(record,['timestamp','@timestamp']);
 const eventId=first(record,['data.win.system.eventID','data.win.system.eventId','data.win.system.event_id','win.system.eventID']);
 const srcIp=clean(first(record,['data.win.eventdata.ipAddress','data.srcip','srcip','source.ip','src_ip']));
 const srcPort=clean(first(record,['data.win.eventdata.ipPort','data.srcport','srcport','source.port','src_port']));
 const dstIp=clean(first(record,['data.dstip','dstip','destination.ip','dst_ip']));
 const dstPort=clean(first(record,['data.dstport','dstport','destination.port','dst_port']));
 const targetUser=clean(first(record,['data.win.eventdata.targetUserName','data.dstuser','user.name','username']));
 const targetDomain=clean(first(record,['data.win.eventdata.targetDomainName','data.domain','user.domain']));
 const subjectUser=clean(first(record,['data.win.eventdata.subjectUserName','data.srcuser']));
 const subjectDomain=clean(first(record,['data.win.eventdata.subjectDomainName']));
 const process=clean(first(record,['data.win.eventdata.image','data.win.eventdata.newProcessName','data.win.eventdata.processName','data.process.name','process.name']));
 const eMs=isoMs(eventTimestamp),aMs=isoMs(alertTimestamp),skewMs=eMs!=null&&aMs!=null?eMs-aMs:null;
 return {...record,
  '@timestamp':eventTimestamp,timestamp:eventTimestamp,
  message:first(record,['data.win.system.message','full_log','message','rule.description','data.message','data.win.eventdata.message']),eventId,
  host:first(record,['agent.name','data.win.system.computer','host.name','hostname']),provider:first(record,['data.win.system.providerName','data.win.system.provider','provider']),channel:first(record,['data.win.system.channel','channel']),
  user:targetUser||subjectUser,domain:targetDomain||subjectDomain,targetUser,targetDomain,subjectUser,subjectDomain,
  workstation:clean(first(record,['data.win.eventdata.workstationName','data.workstation','workstation'])),logonType:clean(first(record,['data.win.eventdata.logonType','data.logonType'])),authenticationPackage:clean(first(record,['data.win.eventdata.authenticationPackageName','data.authenticationPackageName'])),logonProcess:clean(first(record,['data.win.eventdata.logonProcessName','data.logonProcessName'])),
  failureStatus:clean(first(record,['data.win.eventdata.status','data.status'])),failureSubStatus:clean(first(record,['data.win.eventdata.subStatus','data.subStatus'])),failureReason:clean(first(record,['data.win.eventdata.failureReason','data.failureReason'])),
  srcIp,srcip:srcIp,srcPort,srcport:srcPort,dstIp,dstip:dstIp,dstPort,dstport:dstPort,
  protocol:first(record,['data.protocol','data.proto','network.transport','protocol']),process,parentProcess:first(record,['data.win.eventdata.parentImage','data.win.eventdata.parentProcessName','data.process.parent.name','process.parent.name']),pid:first(record,['data.win.eventdata.processId','data.win.system.processID','data.win.system.processId','process.pid']),commandLine:first(record,['data.win.eventdata.commandLine','data.commandLine','process.command_line','commandLine']),action:first(record,['data.action','data.event.action','event.action','action']),status:first(record,['data.win.system.severityValue','data.status','event.outcome','status']),url:first(record,['data.url','data.http.url','url.full','url']),httpMethod:first(record,['data.httpMethod','data.http.method','http.request.method','method']),userAgent:first(record,['data.userAgent','data.http.user_agent','user_agent.original','http.user_agent']),hashes:first(record,['data.win.eventdata.hashes','data.hashes','process.hash','hash']),severity:first(record,['rule.level','event.severity','severity','level']),source:first(record,['decoder.name','location','rule.groups.0'])||'wazuh',
  eventTimestamp,alertTimestamp,timestampSkewMs:skewMs,agentIp:clean(agent.ip),wazuhRuleId:text(rule.id),wazuhRuleLevel:Number(rule.level||0),
  wazuh:{ruleId:text(rule.id),ruleLevel:Number(rule.level||0),ruleDescription:text(rule.description),ruleGroups:Array.isArray(rule.groups)?rule.groups.map(text):[],mitre:rule.mitre||null,agentId:text(agent.id),agentName:text(agent.name),agentIp:text(agent.ip),managerName:text(manager.name),decoderName:text(decoder.name),location:text(record.location),alertId:text(record.id),windowsEventId:text(eventId),providerName:text(ws.providerName),computer:text(ws.computer),targetUserName:text(we.targetUserName),targetDomainName:text(we.targetDomainName),subjectUserName:text(we.subjectUserName),subjectDomainName:text(we.subjectDomainName),logonType:text(we.logonType),status:text(we.status),subStatus:text(we.subStatus),failureReason:text(we.failureReason),index:text(record.__wazuhIndex),documentId:text(record.__wazuhDocumentId),eventTimestamp:text(eventTimestamp),alertTimestamp:text(alertTimestamp),timestampSkewMs:skewMs}
 };
}
function enrich(events){for(const event of events||[]){const raw=event.raw||{};for(const key of ['targetUser','targetDomain','subjectUser','subjectDomain','workstation','logonType','authenticationPackage','logonProcess','failureStatus','failureSubStatus','failureReason','agentIp','eventTimestamp','alertTimestamp','timestampSkewMs','wazuhRuleId','wazuhRuleLevel','wazuh'])if(raw[key]!==undefined)event[key]=raw[key]}}
function prepare(input){const original=text(input).replace(/^\uFEFF/,''),trimmed=original.trim();if(!trimmed)return{text:original,recognized:0,wrapper:'none'};try{const parsed=JSON.parse(trimmed),records=extractRecords(parsed),recognized=records.filter(looksWazuh).length;if(!recognized)return{text:original,recognized:0,wrapper:'none'};return{text:JSON.stringify(records.map(canonicalize)),recognized,wrapper:Array.isArray(parsed)?'array':parsed?.hits?.hits?'opensearch-hits':parsed?.data?.affected_items?'wazuh-api-affected-items':parsed?._source?'opensearch-source':'wazuh-json'}}catch{}const lines=original.split(/\r?\n/);let recognized=0;const mapped=lines.map(line=>{if(!line.trim())return line;try{const record=unwrapHit(JSON.parse(line));if(looksWazuh(record)){recognized++;return JSON.stringify(canonicalize(record))}}catch{}return line});return recognized?{text:mapped.join('\n'),recognized,wrapper:'wazuh-ndjson'}:{text:original,recognized:0,wrapper:'none'}}
function install(engine){if(!engine?.analyzeLogs||engine.__wazuhAdapterInstalled)return engine;const original=engine.analyzeLogs.bind(engine);engine.analyzeLogs=function(input){const prepared=prepare(input),result=original(prepared.text);if(prepared.recognized){enrich(result.events);result.summary={...(result.summary||{}),vendor:'wazuh',wazuh:{adapterVersion:VERSION,recognizedRecords:prepared.recognized,wrapper:prepared.wrapper}}}return result};engine.__wazuhAdapterInstalled=true;return engine}
const api={VERSION,get,first,extractRecords,looksWazuh,canonicalize,prepare,enrich,install};global.DBMWazuhAdapter=api;install(global.DBMLogEngine);
})(typeof window!=='undefined'?window:globalThis);
