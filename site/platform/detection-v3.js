(function(global){
'use strict';
const P=global.DBMPlatform=global.DBMPlatform||{};
const text=v=>String(v??'');const num=v=>Number(v);const get=(e,f)=>f.split('.').reduce((v,k)=>v==null?undefined:v[k],e);
const OPS={equals:(a,b)=>text(a)===text(b),notEquals:(a,b)=>text(a)!==text(b),contains:(a,b)=>text(a).toLowerCase().includes(text(b).toLowerCase()),startsWith:(a,b)=>text(a).toLowerCase().startsWith(text(b).toLowerCase()),endsWith:(a,b)=>text(a).toLowerCase().endsWith(text(b).toLowerCase()),exists:a=>a!==undefined&&a!==null&&text(a)!=='',gt:(a,b)=>num(a)>num(b),gte:(a,b)=>num(a)>=num(b),lt:(a,b)=>num(a)<num(b),lte:(a,b)=>num(a)<=num(b),in:(a,b)=>Array.isArray(b)&&b.map(text).includes(text(a))};
function safeRegex(pattern){pattern=text(pattern);if(!pattern||pattern.length>256)return null;if(/(?:\([^)]*[+*][^)]*\))[+*{]/.test(pattern)||/(?:\.\*|\.\+).*(?:\.\*|\.\+)/.test(pattern))return null;try{return new RegExp(pattern,'i')}catch{return null}}
function validateCondition(c,path='condition'){const errors=[];if(!c||typeof c!=='object')return[`${path}: invalid condition`];if(c.all){if(!Array.isArray(c.all)||!c.all.length)errors.push(`${path}.all: empty group`);else c.all.forEach((x,i)=>errors.push(...validateCondition(x,`${path}.all[${i}]`)));return errors}if(c.any){if(!Array.isArray(c.any)||!c.any.length)errors.push(`${path}.any: empty group`);else c.any.forEach((x,i)=>errors.push(...validateCondition(x,`${path}.any[${i}]`)));return errors}if(c.not)return validateCondition(c.not,`${path}.not`);if(!c.field)errors.push(`${path}: missing field`);if(c.op==='regex'){if(!safeRegex(c.value))errors.push(`${path}: invalid or unsafe regex`)}else if(!OPS[c.op])errors.push(`${path}: unknown operator ${c.op}`);return errors}
function matchCondition(e,c){if(c.all)return c.all.every(x=>matchCondition(e,x));if(c.any)return c.any.some(x=>matchCondition(e,x));if(c.not)return!matchCondition(e,c.not);const a=get(e,c.field);if(c.op==='regex'){const r=safeRegex(c.value);return!!r&&r.test(text(a))}return OPS[c.op]?.(a,c.value)===true}
function ts(e){const t=Date.parse(e.timestamp||'');return Number.isFinite(t)?t:null}
function groupKey(e,fields=[]){return fields.map(f=>text(get(e,f))||'∅').join('|')}
function finding(rule,events,extra={}){const evidence=events.slice(0,50).map(e=>({eventId:e.stableId||e.id,timestamp:e.timestamp||'',source:e.source||'',host:e.host||'',user:e.user||'',message:text(e.message).slice(0,500),provenance:e.provenance||null}));return{id:`FND-${rule.id}-${Math.abs(hash(JSON.stringify(evidence.map(x=>x.eventId))))}`,ruleId:rule.id,name:rule.name,severity:rule.severity||'medium',confidence:rule.confidence||'medium',mitre:rule.mitre||[],tags:rule.tags||[],why:rule.why||rule.description||'Declarative detection conditions matched.',falsePositive:rule.falsePositive||'',remediation:rule.remediation||'',eventIds:events.map(e=>e.stableId||e.id),evidence,...extra}}
function hash(s){let h=0;for(let i=0;i<s.length;i++)h=((h<<5)-h+s.charCodeAt(i))|0;return h}
function runThreshold(rule,events){const cfg=rule.threshold||{},windowMs=Math.max(1000,Number(cfg.windowMs||300000)),threshold=Math.max(1,Number(cfg.count||1)),distinct=cfg.distinctField||'',groups=new Map(),out=[];for(const e of events){if(!matchCondition(e,rule.condition))continue;const t=ts(e);if(t==null)continue;const key=groupKey(e,cfg.groupBy||[]);let g=groups.get(key);if(!g){g=[];groups.set(key,g)}g.push(e);while(g.length&&t-ts(g[0])>windowMs)g.shift();const metric=distinct?new Set(g.map(x=>text(get(x,distinct))).filter(Boolean)).size:g.length;if(metric>=threshold){out.push(finding(rule,[...g],{correlation:true,correlationType:'threshold',windowMs,group:key,threshold,observed:metric,distinctField:distinct||null}));g.length=0}}return out}
function runSequence(rule,events){const stages=rule.sequence?.stages||[],windowMs=Math.max(1000,Number(rule.sequence?.windowMs||300000)),groupBy=rule.sequence?.groupBy||[],state=new Map(),out=[];if(stages.length<2)return out;for(const e of events){const t=ts(e);if(t==null)continue;const key=groupKey(e,groupBy);let s=state.get(key);if(!s||t-s.startedAt>windowMs)s={index:0,startedAt:t,events:[]};const stage=stages[s.index];if(stage&&matchCondition(e,stage)){s.events.push(e);s.index++;if(s.index===stages.length){out.push(finding(rule,[...s.events],{correlation:true,correlationType:'sequence',windowMs,group:key,stages:stages.length}));s={index:0,startedAt:t,events:[]}}state.set(key,s)}return out}
function validateRule(rule){const errors=[];if(!rule?.id)errors.push('missing rule id');if(!rule?.name)errors.push('missing rule name');if(!rule?.condition&&!rule?.sequence)errors.push('missing condition or sequence');if(rule.condition)errors.push(...validateCondition(rule.condition));if(rule.threshold){if(Number(rule.threshold.count)<1)errors.push('threshold count must be >= 1');if(Number(rule.threshold.windowMs)<1000)errors.push('threshold window must be >= 1000 ms')}if(rule.sequence){if(!Array.isArray(rule.sequence.stages)||rule.sequence.stages.length<2)errors.push('sequence requires >= 2 stages');else rule.sequence.stages.forEach((x,i)=>errors.push(...validateCondition(x,`sequence.stages[${i}]`)));if(Number(rule.sequence.windowMs)<1000)errors.push('sequence window must be >= 1000 ms')}return{valid:errors.length===0,errors}}
function runRule(rule,events=[]){const v=validateRule(rule);if(!v.valid)return{valid:false,errors:v.errors,findings:[],elapsedMs:0};const started=performance?.now?.()??Date.now();let findings;if(rule.sequence)findings=runSequence(rule,events);else if(rule.threshold)findings=runThreshold(rule,events);else findings=events.filter(e=>matchCondition(e,rule.condition)).map(e=>finding(rule,[e]));return{valid:true,errors:[],findings,elapsedMs:Math.round((performance?.now?.()??Date.now())-started)}}
const builtins=[
{id:'CORR-AUTH-SPRAY',name:'Authentication spray pattern',severity:'high',confidence:'high',mitre:['T1110.003'],tags:['windows','linux','authentication'],description:'Repeated authentication failures from one source against multiple accounts.',why:'The same source produced repeated failures against several distinct usernames in a bounded window.',falsePositive:'Shared gateways, jump hosts or misconfigured identity clients.',remediation:'Validate source ownership, targeted accounts and any subsequent successful authentication.',condition:{any:[{field:'eventId',op:'equals',value:'4625'},{field:'message',op:'regex',value:'(?:failed password|authentication failure|invalid user)'}]},threshold:{count:3,windowMs:180000,groupBy:['srcIp'],distinctField:'user'}},
{id:'CORR-AUTH-SUCCESS-AFTER-FAIL',name:'Successful authentication after repeated failures',severity:'high',confidence:'medium',mitre:['T1110','T1078'],tags:['authentication','sequence'],description:'Success follows a failure burst from the same source.',why:'Repeated failed logons were followed by a successful logon from the same source in a short time window.',falsePositive:'Users correcting a password after several mistakes.',remediation:'Confirm user, source IP, device and whether the successful session was expected.',sequence:{windowMs:300000,groupBy:['srcIp'],stages:[{any:[{field:'eventId',op:'equals',value:'4625'},{field:'message',op:'regex',value:'failed password|authentication failure'}]},{any:[{field:'eventId',op:'equals',value:'4624'},{field:'message',op:'regex',value:'accepted password|successful logon'}]}]}},
{id:'CORR-SERVICE-PROCESS',name:'Service installation followed by process execution',severity:'high',confidence:'medium',mitre:['T1543.003','T1059'],tags:['windows','persistence','sequence'],description:'New service installation followed by process execution on the same host.',why:'Service installation and subsequent process creation occurred on the same host inside a bounded window.',falsePositive:'Software deployment and endpoint-management tooling.',remediation:'Review service binary path, signer, parent/child process lineage and change authorization.',sequence:{windowMs:180000,groupBy:['host'],stages:[{field:'eventId',op:'equals',value:'7045'},{any:[{field:'eventId',op:'equals',value:'4688'},{field:'eventId',op:'equals',value:'1'}]}]}}
];
P.detectionsV3={OPS,safeRegex,validateCondition,matchCondition,validateRule,runRule,builtins,runAll(events,rules=builtins){return rules.flatMap(r=>runRule(r,events).findings)}};
})(window);
