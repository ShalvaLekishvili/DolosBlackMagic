(function(){'use strict';
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function init(){const dash=$('#view-dashboard');if(!dash||$('#analystLaunch'))return;const hero=dash.querySelector('.hero');if(hero){const copy=hero.querySelector('div');if(copy){copy.querySelector('.eyebrow').textContent='LOCAL-FIRST DFIR / SOC WORKBENCH';copy.querySelector('h1').innerHTML='Investigate faster.<br><em>Keep evidence local.</em>';copy.querySelector('p').textContent='Ingest telemetry, validate detections, correlate evidence, triage findings and build incidents without sending analyst data to a backend.'}}
 const launch=document.createElement('section');launch.id='analystLaunch';launch.className='analyst-launch';launch.innerHTML=`<div class="launch-head"><div><span class="kicker">ANALYST START</span><h2>Open a workspace</h2></div><span class="launch-state" id="launchState">No telemetry loaded</span></div><div class="launch-grid"><button data-open="intake"><span>01</span><b>Artifact Analysis</b><small>Inspect files, commands and indicators</small></button><button id="launchLogs"><span>02</span><b>Event Explorer</b><small>Normalize and search security telemetry</small></button><button id="launchDetect"><span>03</span><b>Detection Studio</b><small>Test rules and triage findings</small></button><button id="launchOps"><span>04</span><b>SOC Operations</b><small>Correlate entities and data quality</small></button></div>`;
 const metrics=dash.querySelector('.metrics');metrics?.insertAdjacentElement('afterend',launch);launch.querySelector('[data-open="intake"]')?.addEventListener('click',()=>document.querySelector('[data-view="intake"]')?.click());$('#launchLogs')?.addEventListener('click',()=>$('#blacklogNav')?.click());$('#launchDetect')?.addEventListener('click',()=>$('#socNav')?.click());$('#launchOps')?.addEventListener('click',()=>$('#opsNav')?.click());refresh();window.addEventListener('dbm:log-analysis',refresh);window.addEventListener('storage',refresh)}
function refresh(){const state=$('#launchState');if(!state)return;const r=window.DBMState?.logResult;if(r){const hi=(r.summary?.findingSeverity?.critical||0)+(r.summary?.findingSeverity?.high||0);state.textContent=`${r.events.length} events · ${r.findings.length} findings · ${hi} high priority`;state.classList.toggle('hot',hi>0);return}let incidents=0;try{incidents=window.DBMSOC?.incidents?.().filter(i=>i.status!=='closed').length||0}catch{}state.textContent=incidents?`${incidents} open incident${incidents===1?'':'s'}`:'No telemetry loaded'}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,0));else setTimeout(init,0);
})();
